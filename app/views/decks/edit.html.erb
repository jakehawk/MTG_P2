<h1><%= @deck.name %></h1>

<%= link_to "Done Editing", deck_path(@deck) %>

<br>
New Card:
<%= form_for [@deck, @spell], :action=>"add_spell" do |f| %>
	<%= f.text_field :name %>
	<%= number_field :qty, :qty, in: 1..4 %>
	<%= f.submit "Add Card" %>
<% end %>

Current Deck:<br>
<% @deck.spells.each_slice(4).each do |row| %>
	<div class="row">
		<% row.each do |spell| %>
			<div class="col-md-3">
				<strong><%=spell.name%></strong>
				<div class="delete"> <%= link_to "Delete", deck_path(@deck), method: :delete, data: {confirm: "Are you sure you want to delete this spell?"} %> </div>
				<img src="<%= spell.img_url %>" /> 
			</div>
		<% end %>
	</div>
<% end %>



